"RDT2.2的核心改进是：

1. **取消NACK**，只使用一种反馈类型ACK
2. **重复ACK机制**：接收方收到错误包时，重发上一个正确的ACK
3. **发送方通过检测重复ACK来判断错误**，而不是等待NACK

## 第二部分：接收端实现讲解（5-7分钟）

**【切换到 TCP_Receiver.java 代码】**

### 2.1 关键变量：lastCorrectSeq

**【指向代码第18行】**

```java
int lastCorrectSeq = 0; //记录上一个正确接收的包的序号（用于RDT2.2重复ACK）
```

**【你可以这样讲解】**

"这是RDT2.2新增的关键变量：**lastCorrectSeq**。

**作用**：记录上一次成功接收的数据包序号。

**为什么需要它**？

- 当收到损坏的包时，接收方需要知道『上一个正确的包是哪个』
- 这样才能重发那个包的ACK，形成『重复ACK』
- 发送方收到重复ACK后，就知道当前包出错了

**初始值**：设为0，表示还没有成功接收过任何包。"

你问得很好！让我解释一下`receivedAck != 0`这个条件的作用。

## 问题分析

看第100行的条件：
```java
else if (receivedAck == lastAckReceived && receivedAck != 0)
```


这个`receivedAck != 0`是为了**避免误判初始状态**。

## 场景分析

### 为什么需要`!= 0`？

**问题场景**：如果没有这个条件会发生什么？

```java
// 初始状态
lastAckReceived = 0;  // 初始值

// 发送第一个包 seq=1
发送方发送 seq=1 → 接收方收到但损坏
               → 接收方发送重复ACK(ack=0)  // lastCorrectSeq初始也是0
               → 发送方收到 ACK(ack=0)
               → receivedAck=0, lastAckReceived=0
               → receivedAck == lastAckReceived ✓ （相同！）
               → 如果没有 != 0 条件，会被误判为"重复ACK"
```


实际上这**不是真正的重复ACK**，因为：
- 这是第一次通信
- lastAckReceived=0只是初始值，不是真实收到过的ACK
- 接收方的lastCorrectSeq=0也是初始值

## 更好的理解方式

`receivedAck != 0`的含义是：**确保这是一个有效的ACK值，而不是初始状态的0**


## 当前代码的逻辑

你当前的代码用`!= 0`的假设是：
- ✅ 序号从1开始（dataIndex * appData.length + 1）
- ✅ 永远不会有ACK(ack=0)
- ✅ 所以0只可能是初始状态

**这在你的代码中是成立的！** 因为第一个包的seq=1，所以第一个有效的ACK是ack=1。

## 总结

`receivedAck != 0`是为了：
- 排除初始状态（lastAckReceived=0还没收到过真实ACK）
- 避免把"第一次收到ACK(ack=0)"误判为"重复ACK"
- 前提是你的协议中永远不会有seq=0的包

**这个条件在你当前的实现中是合理的**，但如果要写得更清晰，建议改用方案1或方案2。

需要我帮你改进这个逻辑吗？

## 第三部分：发送端实现讲解（7-10分钟）

**【切换到 TCP_Sender.java 代码】**

**【你可以这样说】**

"发送端的关键是如何检测重复ACK，这是RDT2.2的另一个核心。"