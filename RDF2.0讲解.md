RDT2.0可靠数据传输协议的实现。这个实验主要解决的问题是：**如何在存在位错误的不可靠信道上实现可靠的数据传输**。

RDT2.0的核心机制是**检错、反馈、重传**，我会从发送端、接收端和校验和三个模块来讲解我的代码设计。

![image-20251230140857356](C:\Users\liuhe\AppData\Roaming\Typora\typora-user-images\image-20251230140857356.png)

RDT2.0假设信道只会出现位错误，不会丢包或乱序。所以我们只需要处理两种情况：

1. **数据包传输过程中被损坏** - 接收方通过校验和检测，发送NACK
2. **ACK包被损坏** - 发送方通过确认号校验，决定是否重传

协议使用**停等机制**：发送方每次只发送一个包，收到正确的ACK后才能发送下一个包。"

## 第二部分：校验和模块讲解（3-4分钟）

**【切换到 CheckSum.java 代码】**

这个方法负责计算TCP数据包的校验和。我选择使用**CRC32算法**，而不是简单的求和校验

**具体步骤**：

- 第13-16行：提取需要校验的字段（序号、确认号、数据）
- 第19-24行：使用CRC32算法逐字段累加计算
- 第27行：返回校验和值

## 第三部分：接收端模块讲解（5-7分钟）

**【切换到 TCP_Receiver.java 代码】**

接下来是接收端的实现，这是RDT2.0协议『检错』和『反馈』机制的体现

## 第四部分：发送端模块讲解（7-10分钟）

**【切换到 TCP_Sender.java 代码】**

现在讲解发送端实现，这是RDT2.0『重传』机制的核心